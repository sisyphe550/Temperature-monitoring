# 待验证、待决策与风险登记

更新日期：2026-09-14。所有事项当前均未关闭。“阻塞”表示相关设计／验收不能冻结，不表示本轮文档整理被阻塞。

| ID | 问题 | 类型／影响 | 关闭所需证据或决定 |
|---|---|---|---|
| OQ-01 | M4 Air 能读取哪些 CPU、SSD、Battery 温度，Package 与逐核如何映射 | 关键能力；阻塞采集承诺 | V-02/V-03 实测与映射说明；主指标定义 |
| OQ-02 | 50 ms～1 s 的可验收语义、实际调度误差、新鲜度和有效率 | 阻塞采样验收 | V-04 计时；明确轮询与测量区别；给出验收口径 |
| OQ-03 | 首发覆盖哪些 Air 代际、尺寸、系统；不同 provider 重复／切换如何标识 | 兼容与身份 | 实机矩阵、来源优先级及映射版本策略 |
| OQ-04 | 教师是否要求采集→数据库→加工→数据库→UI；实时折线从内存还是 `ema_samples` 读取 | 架构与外部验收冲突 | 确认课程验收要求；确定最终实时路径 |
| OQ-05 | 哪些指标必需；不支持、权限不足、单项故障时继续还是退出 | 阻塞故障语义 | 明确能力与故障矩阵、降级／退出产品行为 |
| OQ-06 | Raw 聚合修正、样本／时间平均、tau、趋势窗口与阈值、空窗口和晚到数据 | 阻塞算法详细验收 | 确定数学口径、触发、初始化、缺口与重试幂等规则 |
| OQ-07 | 各数据文件／WAL／日志预算、TTL 物理清理宽限 | 非功能量化暂缓；影响有界承诺 | 测量及预算；区分查询 TTL 与物理回收 |
| OQ-08 | 日历／单调时钟、休眠恢复、系统校时、缺口阈值 | 生命周期和时间正确性 | 时钟策略及睡眠／校时测试；定义 EMA／趋势重置 |
| OQ-09 | 调用超时、底层阻塞取消、权限、单进程还是辅助进程 | 采集隔离和可靠性 | 普通用户与发布配置实测；阻塞／崩溃边界 |
| OQ-10 | 批量入库间隔、最大延迟、队列容量、满队列、事务与重试幂等 | 存储与实时性 | 数据链测试及明确的数据交付／丢失策略 |
| OQ-11 | Fatal 展示与退出时机、报告写入失败兜底、日志格式／轮转／目录 | 运维与用户可观察性 | 明确交互、收尾时限、诊断文件策略 |
| OQ-12 | 视觉、默认图表范围、任意历史范围、跨层查询、缓存过期、窗口关闭行为 | UI 详细设计 | 视觉评审、交互与查询规格 |
| OQ-13 | CPU、内存、能耗、磁盘和长时间增长的量化门槛 | 用户已暂缓非功能指标 | 后续需要保证性能时再明确；当前不编造数值 |
| OQ-14 | Swift／Xcode 最低版本、SQLite 封装、schema、索引、WAL与同步级别 | 工具链与存储细节 | 冻结版本、依赖许可、性能及耐久性决定 |
| OQ-15 | GitHub 仓库、主分支、审批人、阻塞标签、CI 与实机资源 | 开发流程落地 | 仓库与规则配置、测试运行环境 |
| OQ-16 | 应用名称、Bundle ID、签名证书、ZIP/DMG、更新与分发渠道 | 发布设计 | 身份与发布方案；不加入未经确认的自启动／自动更新 |

## 处理顺序

首轮先解决 OQ-01/OQ-02/OQ-09，并同步确认 OQ-04/OQ-05。其结果决定采集接口、主指标、采样承诺和系统行为。随后完成算法、存储和 UI 细化。

非功能指标暂缓量化是当前用户选择，本轮不将推荐值包装成硬性门槛。若后续要求证明“资源不会无限增长”，则需要对应预算或可检测的有界性标准。

## 风险与应对

| 风险 | 影响 | 当前应对方向 |
|---|---|---|
| 私有接口或传感器键随系统变化 | 指标失效或误标 | 适配层隔离、显式能力探测、机型／系统矩阵 |
| 相同语义由不同来源以不同单位返回 | 错误温度 | 按来源解码、保留原始值／单位证据 |
| 重试与高频调度叠加 | 队列增长、耗能与退出延迟 | 单通道有界调度与调用超时设计 |
| EMA 后再求 max | 丢失已采集峰值 | Raw 独立聚合 |
| 数据库清理受阻 | 磁盘增长 | 检查点、短事务、容量观测与失败策略 |
| 只看模拟测试或一个小时运行 | 错误宣称兼容与稳定 | 实机测试＋跨保留周期运行 |
| 文档中混入旧结论 | 后续实现错误 | 状态、来源、纠正记录及追踪校验 |

## 关闭记录

目前没有事项已关闭。关闭时记录结果、决定人／依据、证据文件、环境、日期以及影响的 REQ／TC 编号；建议不删除原问题条目，以便追踪。
