# 项目上下文

更新日期：2026-09-17。状态：已确认范围＋推荐设计。文档入口：[README](README.md)。

## 目的与背景

这是一个学校实验项目。最初教学链路为：从本机温度传感器读取多项数据，写入数据库，由加工构件取数计算并写回，前端从数据库取数展示。后续讨论选择了本地 macOS 原生应用、SQLite、内存实时路径和分级历史路径。

**教学要求的数据库往返链路与实时内存路径仍有一个外部验收问题：** 用户已经接受附件中的实时数据路径，但教师是否要求每次实时展示也必须先经数据库，目前没有确认。登记为 OQ-04，不能自行声称该要求已被教师取消。

## 已确认边界

| 维度 | 当前内容 | 来源 |
|---|---|---|
| 硬件 | Apple Silicon MacBook Air | C04，替代此前全部 Apple Silicon Mac 的范围 |
| 排除硬件 | Intel Mac、MacBook Pro、台式 Mac | C01＋C04 |
| 系统 | 最低目标 macOS 15.7.3；最高目标为发布时核对的当前正式版 | C01；最高版本落实方式属于设计基线 |
| 部署 | 本地原生应用、无远端业务服务器 | C01 |
| 分发 | 不上架 Mac App Store | C04 |
| 数据库 | SQLite；删除 Docker | C01 |
| 数据生命周期 | 本次运行会话；历史最长 72 小时；重启不恢复监控历史 | C01 |
| 运维证据 | 错误码、独立日志与错误报告 | C01 |
| 不做功能 | 高温告警、登录后自动启动、Web 前端 | C01 |
| 工作位置 | `/Users/sisyphus/Code/Go/Temperature monitoring/` | C05 |

路径名称不构成语言选择。Swift＋少量桥接已用于独立验证原型；模块化单体与原生 UI 仍是生产应用的推荐方案。

## 术语

| 术语 | 本项目定义／限制 |
|---|---|
| Sensor／传感器 | 某个读取接口暴露的温度来源；不默认等于一个 CPU 核心 |
| 热区 | 芯片某区域的温度来源；不等于物理核心，不承诺覆盖全芯片 |
| 派生指标 | 对明确成员集合应用公式的结果；记录定义版本，不冒充独立物理传感器 |
| CPU Package | 用户要求的默认主指标；Apple Silicon 上物理含义及对应来源尚未确定 |
| CPU Core | 物理核心；只有验证映射后才能将传感器标为对应核心 |
| Raw Sample | 应用取得的未经 EMA 平滑的读数；不保证是芯片原始 ADC 值，也不保证是刚生成的测量 |
| EMA | 使用实际时间差计算的指数移动平均 |
| Ring Buffer | 有界内存环形缓冲区；按传感器维护最近数据 |
| 聚合窗口 | 1 秒、10 秒、1 分钟等时间区间；保存 min/max/avg/latest/count |
| 轮询间隔 | 应用请求读取的目标时间间隔 |
| 硬件刷新间隔 | 传感器或驱动更新其测量值的间隔，可能不可观测 |
| UI 刷新间隔 | 界面更新显示的间隔，与轮询解耦 |
| 会话 | 一次应用启动到结束；休眠本身不等于新会话，但会产生采样缺口 |
| 持久化 | 将数据写入本地数据库；本项目不要求跨会话恢复历史 |
| Fatal | 按产品策略必须终止本次运行的故障；与单项能力不支持需区分 |

## 内容解释与冲突处理

1. 当前用户明确指令优先于更早的用户选择；用户选择优先于助手建议及附件建议。
2. 事实错误以有证据的纠正为准；纠错不等于替用户决定新的业务规则。
3. 授权助手设计的内容标为设计基线；新的架构建议标为推荐方案。
4. 不把论坛反馈视为 Apple 官方承诺，不把开源实现中的命名视为官方传感器拓扑。
5. 不把文档存在、编译成功、调用成功、语义正确、跨版本稳定混为一项验证。
6. 未确认的性能、精度、时延、资源预算和产品行为集中在 [待决策清单](docs/16-open-questions.md)。

## 项目现状

截至 2026-09-15，已存在 Git 仓库与公开远端 `sisyphe550/Temperature-monitoring`，主分支 `main`。本次按用户授权从最新主分支建立 `feature/v0-sensor-validation`，新增独立只读 Swift/C 原型、测试、CI 与 M4 Air 实测证据。

实测机为 Mac16,13／M4／macOS 15.7.3 (24G419)。已取得候选温度与 SSD SMART 读数；不等于 Package／逐核映射已建立。详见 [报告](docs/validation/2026-09-15-m4-air/validation-report.md)。完整 V0 与生产设计仍未冻结；GitHub 强制门禁尚未完整配置，合并保持待审查。

参考：[来源编号](docs/18-sources.md)、[决策记录](docs/15-decisions-and-corrections.md)。

2026-09-17 按 C08 完成开源项目复核与推荐方案细化，见 [19](docs/19-reference-informed-design.md)。需求修订提案 RC-01～04 尚未替代已确认要求；V0.2 未执行，原有硬件与教学验收缺口继续保留。
